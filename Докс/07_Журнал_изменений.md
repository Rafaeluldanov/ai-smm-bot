# Журнал изменений

## 2026-06-18 — Этап 10 — Автономный режим

Завершён **Этап 10** (поверх Этапов 0–9). Готовые модули связаны в управляемый автономный pipeline с safety-guardrails: выбор тем → генерация → медиа → внешние картинки → согласование → планирование → публикация → аналитика → отчёт. Реальные публикации и AI выполняются только при явном разрешении; сеть в тестах не вызывается.

### Добавлено

- Модели `backend/app/models/autonomous_run.py` (`AutonomousRun`) и `backend/app/models/autonomous_run_step.py` (`AutonomousRunStep`); добавлены в `app/models/__init__.py`.
- Миграция `0009_autonomous_runs` — таблицы `autonomous_runs` и `autonomous_run_steps` + индексы.
- Схемы `backend/app/schemas/autonomous.py` (run/step Read/Create/Update, request, result, report, settings, safety).
- Репозиторий `backend/app/repositories/autonomous_run_repository.py`.
- Safety `backend/app/services/autonomous_safety_service.py` (`AutonomousSafetyService`).
- Pipeline `backend/app/services/autonomous_pipeline_service.py` (`AutonomousPipelineService`) — `run_pipeline`, `run_for_project_slug`, `dry_run_pipeline`, `build_report`; `AutonomousValidationError` (422), `AutonomousRunNotFoundError` (404).
- REST API `backend/app/api/autonomous_runs.py` (`/autonomous-runs`): list/get/steps/report/run/dry-run/run-by-project/run-by-slug.
- Планировщик: `autonomous_weekly_run_job(db, service, project_slug, request=None)` в `app/scheduler/jobs.py`.
- CLI `autonomous_run.py`/`autonomous_report.py`; команды `make autonomous-run`, `make autonomous-dry-run`, `make autonomous-report`.
- Документ `Докс/16_Автономный_режим.md`.
- Тесты Этапа 10 (репозиторий, safety, pipeline, API, планировщик, импорт CLI).

### Изменено

- `backend/app/api/deps.py` — `get_autonomous_safety_service`, `get_autonomous_pipeline_service`; `backend/app/main.py` — подключён роутер `/autonomous-runs`; `Makefile` — команды автономного режима.

### Состояние

`make check` (ruff + mypy на 117 файлах + pytest, **362 теста**) проходит успешно. Миграция `0009` проверена offline-генерацией SQL. Реальные публикации в тестах не выполняются (fake-клиенты); `auto_publish` срабатывает только при явном `allow_auto_publish`.

### Следующий шаг

Все 11 этапов (0–10) реализованы. Дальше — production hardening / MVP-релиз: реальные токены, реальные Telegram/VK-каналы, мониторинг, логирование, секреты, backup, UI/админка, ручное QA.

---

## 2026-06-18 — Этап 9 — Внешние картинки

Завершён **Этап 9** (поверх Этапов 0–8). Реализована backend-логика поиска, проверки лицензий и безопасного использования внешних изображений. Главное правило: внешнее изображение — **не наш кейс**. Реальные стоки и сеть **не** подключены (fake-провайдер), изображения не скачиваются.

### Добавлено

- Модель `backend/app/models/external_image_candidate.py` (`ExternalImageCandidate`); добавлена в `app/models/__init__.py`.
- Миграция `0008_external_image_candidates` — таблица + индексы и **уникальный** (provider, source_url).
- Схемы `backend/app/schemas/external_image.py` (candidate Read/Create/Update, search/review/convert/safety).
- Репозиторий `backend/app/repositories/external_image_repository.py` (upsert, защита approved/rejected, `mark_review_status`).
- Провайдер `backend/app/services/external_image_provider.py` (`BaseExternalImageProvider`, `FakeExternalImageProvider`, `ExternalImageProviderResult`, `ExternalImageProviderError`) + реестр `external_image_provider_registry.py`.
- Политика лицензий `backend/app/services/external_image_license_policy.py` (`build_forbidden_usage`, `evaluate_candidate_safety`, `can_convert_to_media_asset`, `normalize_license_name`).
- Сервис `backend/app/services/external_image_search_service.py` (`ExternalImageSearchService`) — поиск, review, конвертация в `MediaAsset`; `ExternalImageConversionError` (API → 409).
- REST API `backend/app/api/external_images.py` (`/external-images`): list/get/search/search-post/search-topic/safety/review/convert-to-media.
- CLI `search_external_images.py`/`convert_external_image.py`; команды `make search-external-images`, `make convert-external-image`.
- Документ `Докс/15_Внешние_картинки.md`.
- Тесты Этапа 9 (провайдер, политика лицензий, репозиторий, search-сервис, API, импорт CLI).

### Изменено

- `backend/app/api/deps.py` — `get_external_image_provider_registry`, `get_external_image_search_service`; `backend/app/main.py` — подключён роутер `/external-images`; `Makefile` — команды внешних картинок.
- `backend/app/services/post_generation_service.py` — для постов `needs_media` в `generation_notes` добавлена подсказка о поиске внешнего изображения (поведение не изменено).

### Состояние

`make check` (ruff + mypy на 108 файлах + pytest, **331 тест**) проходит успешно. Миграция `0008` проверена offline-генерацией SQL. Реальные стоки, Instagram и AI **не** подключены; изображения не скачиваются.

### Следующий шаг

Переход к **Этапу 10 — Автономный режим**: связать все этапы в единый pipeline (выбор тем → генерация → согласование → планирование → публикация → аналитика) с правилами безопасности и отчётами.

---

## 2026-06-18 — Этап 8 — Аналитика

Завершён **Этап 8** (поверх Этапов 0–7). Реализована backend-логика хранения аналитики публикаций, расчёта эффективности постов/тем/кластеров/проектов и feedback-сигналов для приоритизации тем. Реальные analytics API соцсетей и Instagram **не** подключены; метрики вводятся вручную или берутся у fake-провайдера.

### Добавлено

- Модель `backend/app/models/post_analytics_snapshot.py` (`PostAnalyticsSnapshot`); добавлена в `app/models/__init__.py`.
- Миграция `0007_post_analytics_snapshots` — таблица + индексы (post_id, post_publication_id, project_id, topic_id, platform, snapshot_at, source).
- Схемы `backend/app/schemas/analytics.py` — snapshot Read/Create/Update/Insert, ingest req/result, отчёты по постам/темам/кластерам/проекту, feedback.
- Репозиторий `backend/app/repositories/analytics_repository.py`.
- Метрики `backend/app/services/analytics_metrics.py` — `safe_rate`, `calculate_engagements`, `calculate_ctr`, `calculate_engagement_rate`, `calculate_performance_score`.
- Провайдер `backend/app/services/analytics_provider.py` — `BaseAnalyticsProvider`, `FakeAnalyticsProvider`, `AnalyticsProviderError`.
- Сервис `backend/app/services/analytics_service.py` (`AnalyticsService`) — ingest/fetch снимков, отчёты, feedback; `PublicationNotFoundError`/`AnalyticsInputError`.
- Сервис связи `backend/app/services/topic_analytics_feedback_service.py` (`TopicAnalyticsFeedbackService`).
- REST API `backend/app/api/analytics.py` (`/analytics`): snapshots CRUD, ingest/fetch по публикации, отчёты post/topic/cluster/summary/feedback.
- Планировщик: `collect_publication_analytics_job(db, service, publication_ids=None)` в `app/scheduler/jobs.py`.
- CLI `ingest_analytics.py`/`analytics_report.py`; команды `make ingest-analytics`, `make analytics-report`.
- Документ `Докс/14_Аналитика.md`.
- Тесты Этапа 8 (метрики, репозиторий, провайдер, сервис, feedback-сервис, API, планировщик, импорт CLI).

### Изменено

- `backend/app/api/deps.py` — `get_analytics_provider`, `get_analytics_service`; `backend/app/main.py` — подключён роутер `/analytics`; `Makefile` — команды аналитики.

### Состояние

`make check` (ruff + mypy на 98 файлах + pytest, **296 тестов**) проходит успешно. Миграция `0007` проверена offline-генерацией SQL. Реальные analytics API соцсетей, Instagram и AI **не** подключены.

### Следующий шаг

Переход к **Этапу 9 — Поиск внешних картинок**: поиск изображений с учётом лицензий, метаданные внешних картинок, запрет выдавать внешнее за собственный кейс.

---

## 2026-06-18 — Этап 7 — Автопостинг

Завершён **Этап 7** (поверх Этапов 0–6). Реализована backend-логика планирования и публикации одобренных постов в Telegram и VK через изолированные (мокабельные) клиенты. Реальные сетевые вызовы и Instagram **не** подключены; в тестах — только fake-клиенты.

### Добавлено

- Модель `backend/app/models/post_publication.py` (`PostPublication`) — публикация поста по платформам (post_id, project_id, platform, target_id, status, external_post_id, external_url, scheduled_at, published_at, error_message, attempts, payload); добавлена в `app/models/__init__.py`.
- Миграция `0006_post_publications` — таблица `post_publications` + индексы и **уникальный** (post_id, platform).
- Схемы `backend/app/schemas/post_publication.py` — `PostPublicationRead/Create/Update`, `PostScheduleRequest`, `PostPublishRequest`, `PublishDueRequest`, `PostPublishResult`, `DuePublicationsResult`.
- Репозиторий `backend/app/repositories/post_publication_repository.py` — `get_by_id`, `get_by_post_and_platform`, `list_publications`, `create/update`, `upsert_publication_schedule`, `list_due_publications`.
- Структуры публикации `backend/app/integrations/publishing.py` — `PublishRequest`, `PublishResponse`, `PublishError`, `PublishingClient` (Protocol), `FakePublishingClient`.
- Безопасные клиенты `TelegramPublishingClient` (`telegram/client.py`) и `VKPublishingClient` (`vk/client.py`) — без сети; реестр `PublicationPlatformRegistry` (`publication_platform_registry.py`, `UnknownPlatformError`).
- Сервис `backend/app/services/post_publication_service.py` (`PostPublicationService`) — `schedule_post`, `publish_post`, `publish_due_publications`, `build_publish_request`; `PostNotPublishableError` (API → 409).
- REST API `backend/app/api/post_publications.py` (`/post-publications`): `list`, `get`, `schedule/{post_id}`, `publish/{post_id}`, `publish-due`, `PATCH /{publication_id}`.
- Планировщик: `publish_due_publications_job(db, service, now=None)` в `app/scheduler/jobs.py`.
- CLI `schedule_post.py`/`publish_post.py`/`publish_due.py`; команды `make schedule-post`, `make publish-post`, `make publish-due`.
- Настройки `TELEGRAM_DEFAULT_CHANNEL_ID`, `VK_DEFAULT_GROUP_ID` (config + `.env.example`).
- Документ `Докс/13_Автопостинг.md`.
- Тесты Этапа 7 (репозиторий, клиенты/реестр, сервис, API, планировщик, импорт CLI).

### Изменено

- `backend/app/api/deps.py` — `get_publication_platform_registry`, `get_post_publication_service`; `backend/app/main.py` — подключён роутер `/post-publications`; `Makefile` — команды автопостинга.

### Состояние

`make check` (ruff + mypy на 86 файлах + pytest, **263 теста**) проходит успешно. Миграция `0006` проверена offline-генерацией SQL. Реальные Telegram/VK API, Instagram и сеть в тестах **не** используются.

### Следующий шаг

Переход к **Этапу 8 — Аналитика**: сбор метрик публикаций (охваты/реакции/клики), снимки статистики, влияние на `priority_score` тем.

---

## 2026-06-18 — Этап 6 — Согласование постов

Завершён **Этап 6** (поверх Этапов 0–5). Реализована backend-логика согласования черновиков: журнал действий, сервис решений (одобрить/отклонить/доработать/вернуть в черновик), ручная правка и таймлайн. Реальный Telegram Bot API **не** подключён — подготовлен только интерфейсный слой (preview-карточка с кнопками). Публикация и автопостинг не выполняются.

### Добавлено

- Модель `backend/app/models/post_review_action.py` (`PostReviewAction`) — журнал согласования (`post_id`, `action`, `from_status`, `to_status`, `comment`, `actor_name`, `actor_role`, `payload`); добавлена в `app/models/__init__.py`.
- Миграция `0005_post_review_actions` — таблица `post_review_actions` + индексы `post_id`/`action`/`created_at`.
- Схемы `backend/app/schemas/post_review.py` — `PostReviewActionRead/Create`, `PostReviewDecisionRequest`, `PostReviewEditRequest`, `PostReviewCommentRequest`, `PostReviewTimeline`, `PostReviewCard`.
- Репозиторий `backend/app/repositories/post_review_repository.py` — `create_review_action`, `list_review_actions`, `get_last_review_action`, `count_review_actions`.
- Сервис `backend/app/services/post_review_service.py` (`PostReviewService`) — `submit_for_review`, `approve_post`, `reject_post`, `request_changes`, `return_to_draft`, `edit_post_texts`, `add_comment`, `get_timeline`, `build_review_card`; исключение `ReviewActionNotAllowedError` (API → 409).
- Заглушка `backend/app/integrations/telegram/review_interface.py` (`TelegramReviewInterface`, `TelegramReviewMessage`, `TelegramReviewButton`) — строит карточку и кнопки без отправки.
- REST API `backend/app/api/post_reviews.py` (`/post-reviews`): `card`, `timeline`, `telegram-preview`, `submit`, `approve`, `reject`, `request-changes`, `return-to-draft`, `edit`, `comment`.
- CLI `backend/app/scripts/review_post.py`; команды `make review-post`, `make approve-post`, `make reject-post`.
- Документ `Докс/12_Согласование_постов.md`.
- Тесты Этапа 6 (репозиторий журнала, сервис, Telegram-интерфейс, API, импорт CLI).

### Изменено

- `backend/app/api/deps.py` — `get_post_review_service`; `backend/app/main.py` — подключён роутер `/post-reviews`; `Makefile` — команды согласования.

### Состояние

`make check` (ruff + mypy на 78 файлах + pytest, **231 тест**) проходит успешно. Миграция `0005` проверена offline-генерацией SQL. Реальный Telegram Bot API, публикация и AI **не** подключены.

### Следующий шаг

Переход к **Этапу 7 — Автопостинг**: публикация одобренных постов по расписанию (Telegram-канал, VK-группа), планировщик, `scheduled_at`/`published_at`, обработка ошибок.

---

## 2026-06-18 — Этап 5 — Генерация постов

Завершён **Этап 5** (поверх Этапов 0–4). Выбранная тема (`Topic`) превращается в черновик `Post` с отдельными текстами под Telegram, VK и Instagram, хэштегами, CTA, SEO-ключами и подобранным медиа. Реальный AI и сеть **не** используются — тексты собираются по детерминированным шаблонам форматов; автопостинг и согласование через Telegram не выполняются.

### Добавлено

- `backend/app/services/post_template_taxonomy.py` — таксономия форматов `expert`/`product`/`technology`/`case`/`faq`/`selling` (`purpose`/`structure`/`tone`/`cta_type`); функции `get_available_formats`, `get_template_for_format`, `infer_format_from_topic`.
- `backend/app/services/post_text_helpers.py` — `clean_hashtag`, `build_hashtags` (тематические + брендовые `#teeon`/`#фабрикасувениров`), `build_cta` (по 3 CTA для TEEON и «Фабрики сувениров»), `shorten_text`, `get_brand_name`.
- `backend/app/services/post_media_selection_service.py` — `PostMediaSelectionService.select_media_for_topic`: берёт только `approved`/`approved_video`, сопоставляет по тегам (кластер + слова заголовка), внешние стоки — лишь при подтверждённой лицензии; нет подходящего → `None` + warning.
- `backend/app/services/post_status_service.py` — статусы `draft`, `needs_review`, `approved`, `scheduled`, `published`, `rejected`, `needs_media`; `get_allowed_post_statuses`, `can_transition`, `validate_transition`; исключения `InvalidPostStatusError`/`InvalidPostStatusTransitionError`.
- `backend/app/repositories/post_repository.py` — `get_post_by_id`, `list_posts`, `create_post`, `update_post`, `update_post_status`, `get_existing_post_for_topic`, `PostNotFoundError`.
- Схемы в `backend/app/schemas/post.py` — `PostUpdate`, `PostStatusUpdate`, `PostGenerationRequest`, `PostGenerationResult`, `WeeklyPostGenerationRequest`, `WeeklyPostGenerationResult`.
- REST API `backend/app/api/posts.py` (префикс `/posts`, статические пути до `/{post_id}`): `GET /posts`, `POST /posts/generate/topic/{topic_id}` (404), `POST /posts/generate/weekly-plan` (404), `GET /posts/{post_id}` (404), `PATCH /posts/{post_id}`, `PATCH /posts/{post_id}/status` (404/422/409).
- Миграция `0004_post_indexes` (индекс `ix_posts_status`).
- CLI `backend/app/scripts/generate_post.py` и `generate_weekly_posts.py`; команды `make generate-post topic_id=1` и `make generate-weekly-posts project_slug=teeon`.
- Документ `Докс/11_Генерация_постов.md`.
- Тесты Этапа 5 (таксономия форматов, помощники текста, подбор медиа, статусы, репозиторий, `PostGenerationService`, API `/posts`, импорт CLI).

### Изменено

- `backend/app/services/post_generation_service.py` — заглушка Этапа 0 заменена полноценным `PostGenerationService` (`generate_post_for_topic`, `generate_post_from_topic_object`, `generate_weekly_posts`, `generate_platform_texts`).
- `backend/app/models/post.py` — статус-комментарий приведён к набору Этапа 5, на `status` добавлен индекс.
- `backend/app/api/deps.py` — `get_post_generation_service`; `backend/app/main.py` — подключён роутер `/posts`; `Makefile` — команды генерации.

### Состояние

`make check` (ruff + mypy на 71 файле + pytest, **196 тестов**) проходит успешно. Миграция `0004` проверена offline-генерацией SQL (`CREATE INDEX ix_posts_status`). Реальный AI, автопостинг и согласование через Telegram **не** подключены.

### Следующий шаг

Переход к **Этапу 6 — Согласование**: Telegram-интерфейс ревью, кнопки approve/rewrite/reject, переходы статусов `Post`, журнал согласований.

---

## 2026-06-18 — Этап 4 — Выбор тем

Завершён Этап 4: бот получил рабочую основу для самостоятельного выбора тем и формирования недельного контент-плана.

Добавлено:

- `topic_taxonomy` — словари тематических кластеров.
- `market_signal_provider` — статический источник market/SEO-сигналов.
- `TopicSelectionService`.
- `Topic repository`.
- Миграция `0003_topic_unique_project_title`.
- API `/topics`.
- Команды `make select-topics` и `make content-plan`.
- Weekly content plan на 3 публикации.
- Расчёт `priority_score`.
- Учёт `business_priorities`.
- Учёт `media_readiness_score`.
- Формирование `explanation`.
- Тесты Этапа 4.

Проверки ранее подтверждали: `make check` — `151 passed`.

Ограничения Этапа 4:

- реальный Wordstat/Google Trends не подключён;
- реальный AI не подключён;
- генерация текстов постов не выполняется;
- автопостинг не выполняется.


Все значимые изменения проекта «AI-SMM-бот» фиксируются в этом файле в обратной хронологии (новые записи — сверху).

Формат записи:
- **Дата** в формате `ГГГГ-ММ-ДД`.
- **Заголовок** этапа или изменения.
- **Пункты** — что именно было сделано (добавлено, изменено, удалено, исправлено).

---

## 2026-06-18 — Этап 3 — Анализ медиа

Завершён **Этап 3** (поверх Этапов 0, 1 и 2). Поверх синхронизированных медиаресурсов выстроен слой **анализа медиа**: словари терминов и морфологическое сопоставление, обогащённые теги с оценкой уверенности и флагом ручной проверки, сервисы анализа/ретегирования и управления статусами, REST API и CLI, а также задел под будущий AI-анализ изображений. Миграции БД не потребовались — используются существующие поля `tags`/`status`/`source_type`/`license_type`.

### Добавлено

**Словари терминов:**
- `backend/app/services/media_taxonomy.py`: словари `PRODUCT_TERMS`, `TECHNOLOGY_TERMS`, `DETAIL_TERMS` (жаккард / бирка / шеврон отнесены к деталям), `MATERIAL_TERMS`, `COLOR_TERMS`, `USE_CASE_TERMS`, `AUDIENCE_TERMS`, `PRODUCTION_WORDS` и правила определения категорий.
- Функции: `normalize_text` (нижний регистр, `ё→е`, удаление расширения файла, замена `_ - .` на пробелы), `extract_keywords_by_taxonomy(text)`, `build_topics_from_tags(tags, project_slug)`, `build_seo_keywords_from_tags(tags, project_slug)`, `calculate_tag_confidence(tags)`.
- Морфологическое сопоставление: точное совпадение токена; по префиксу (`жаккардами` → `жаккард`); по общему префиксу с расхождением в хвосте ≤2 символов (`шелкографией` → `шелкография`); многословные термины — как подстрока; короткие коды (`dtf`, `dtg`, `uv`, `hr`, `b2b`) — только точное совпадение.

**MediaAnalysisService:**
- `backend/app/services/media_analysis_service.py`: зависимости `MediaTaggingService`, `MediaStatusService`, репозитории. Методы `analyze_media_asset(db, id, save=True)`, `retag_media_asset(db, id)`, `retag_project_media(db, project_id)`, `retag_project_media_by_slug(db, slug)`, `get_tags_summary(db, project_id=None)`, `suggest_shooting_tasks(db, project_id)`.
- Сводка считает частоты по `products`/`technologies`/`details`/`materials`/`colors`/`categories`/`use_cases`/`audiences`. Рекомендации по досъёмке: для TEEON целевые темы (футболка, худи, шоппер, шелкография, dtf, вышивка, жаккард), для «Фабрики сувениров» (кружка, ручка, пакет, уф-печать, тампопечать, гравировка, шелкография); если по теме одобренных медиа меньше 2 — предлагается задача с папкой `06_Нужно_переснять` и списком кадров.

**MediaStatusService:**
- `backend/app/services/media_status_service.py`: допустимые статусы `new`, `approved`, `approved_video`, `needs_license_review`, `rejected`, `needs_reshoot`, `used`. Функции `get_allowed_statuses`, `can_transition`, `validate_transition`, `update_media_status`.
- Исключения `InvalidMediaStatusError` (неизвестный статус), `InvalidMediaStatusTransitionError` (запрещённый переход). Правила переходов: `new→{approved, approved_video, needs_license_review, rejected, needs_reshoot}`; `approved→{used, rejected, needs_reshoot}`; `approved_video→{used, rejected, needs_reshoot}`; `needs_license_review→{approved, rejected}`; `needs_reshoot→{approved, rejected}`; `used→{approved, approved_video}`; `rejected→{approved, needs_reshoot}`.

**AI-анализ изображений (задел на будущее):**
- `backend/app/ai/vision.py`: `VisionAnalysisNotConfiguredError`, `VisionAnalysisResult` (dataclass), `BaseVisionAnalyzer` (Protocol с `analyze_image_metadata`), `StubVisionAnalyzer` (всегда бросает `VisionAnalysisNotConfiguredError`). Реальный AI **не подключён** — только задел на будущее.

### Изменено

**MediaTaggingService — обогащённые теги:**
- `backend/app/services/media_tagging_service.py`: метод `analyze_file_name(file_name, project_slug=None, yandex_disk_path=None, source_type=None)` возвращает богатую структуру; `extract_tags_from_file_name` сохранён для совместимости; добавлены `suggest_content_topics`, `suggest_seo_keywords`.
- Структура `tags`: `products`, `technologies`, `details`, `materials`, `colors`, `categories`, `use_cases`, `audiences`, `topics`, `seo_keywords`, `matched_terms`, `confidence` (0.0–1.0), `needs_review` (bool), `review_reasons` (list). `needs_review=true`, если `confidence<0.35`; либо нет ни `products`, ни `technologies`; либо `source_type==external_stock`; либо путь содержит `04_Внешние_картинки_из_интернета`.
- Категории (`categories`): `apparel` (изделие одежды), `souvenirs` (сувенирный продукт), `branding` (есть технология), `production` (слова цех / производство / станок / процесс), `packaging` (упаковка / коробка), `event_merch` (мероприятие / выставка / конференция / event), `external_reference` (`source_type` external_stock или путь внешних картинок).

**Папки сканирования:**
- `backend/app/services/project_media_paths.py`: `get_default_scan_folders` теперь возвращает **5 папок** — добавлена `06_Нужно_переснять`; папка `05_Использовано_в_постах` **не** сканируется.

**Сервис синхронизации:**
- В `backend/app/services/yandex_disk_media_sync_service.py` добавлена классификация `06_Нужно_переснять` → `source_type=internal`, `license_type=company_owned`, `status=needs_reshoot`. В тегирование теперь передаются `project_slug`, `yandex_disk_path`, `source_type` — теги стали богаче. Прежние статусы (`new`/`approved`/`approved_video`/`needs_license_review`) сохранены.

**Схемы:**
- В `backend/app/schemas/media_asset.py` добавлены `MediaAssetStatusUpdate`, `MediaAssetAnalysisResult`, `MediaAssetRetagResult`, `MediaAssetTagsSummary`, `ShootingTaskSuggestion`.

**REST API:**
- В `backend/app/api/media_assets.py` статические маршруты объявлены **до** `/{media_asset_id}`: `GET /media-assets/tags/summary?project_id=` → `MediaAssetTagsSummary`; `GET /media-assets/shooting-suggestions?project_id=` → `list[ShootingTaskSuggestion]` (`404`, если нет проекта); `POST /media-assets/{id}/analyze?save=true` → `MediaAssetAnalysisResult` (`404`); `POST /media-assets/{id}/retag` → `MediaAssetRead` (`404`); `POST /media-assets/retag/project/{project_id}` → `MediaAssetRetagResult` (`404`); `POST /media-assets/retag/slug/{slug}` (`404`); `PATCH /media-assets/{id}/status` (body `{"status": "..."}`) → `MediaAssetRead` (`404` нет актива, `422` неизвестный статус, `409` запрещённый переход).
- Старые эндпоинты (`GET /media-assets`, `GET /media-assets/{id}`, `POST /media-assets/sync/project/{id}`, `POST /media-assets/sync/slug/{slug}`) сохранены.

**CLI и Makefile:**
- Скрипты `backend/app/scripts/retag_media.py` (`--project-slug` или `--project-id`) и `backend/app/scripts/media_summary.py`; команды `make retag-media project_slug=teeon` и `make media-summary project_slug=teeon`. Ретегирование **не** требует `YANDEX_DISK_TOKEN` (данные берутся из БД, Диск не дёргается).

**Тесты:**
- Тесты на **SQLite** (без сети, Docker, AI и реального Яндекс Диска). Покрыто: `media_taxonomy`; расширенный `MediaTaggingService`; `project_media_paths` (5 папок, включая `06`, без `05`); sync (`needs_reshoot`, богатые теги); `MediaStatusService` (статусы и переходы); `MediaAnalysisService` (`analyze` save=false/true, ретегирование проекта, сводка, рекомендации); API `media-assets` Этапа 3; заглушка vision. Старые тесты Этапов 0–2 сохранены. Всего **108 тестов** — все проходят.

### Состояние

Миграции БД на этом этапе **не добавлялись** (структура не менялась; используются существующие поля `tags`/`status`/`source_type`/`license_type`); цепочка миграций `0001→0002` компилируется. `make check` (ruff + mypy на 57 файлах + pytest, 108 тестов) проходит успешно. Live-смоук новых эндпоинтов выполнен успешно. Реальный AI-анализ изображений остаётся заглушкой (`StubVisionAnalyzer`).

### Следующий шаг

Переход к следующему этапу — развитие бизнес-логики поверх обогащённых тегов медиаресурсов.

---

## 2026-06-18 — Этап 2 — Яндекс Диск

Завершён **Этап 2** (поверх Этапов 0 и 1). Реализована интеграция с **Яндекс Диском** (read-only) и синхронизация медиафайлов проектов в сущность `MediaAsset`: клиент REST API, сопоставление папок проектов, репозиторий с идемпотентным upsert, миграция индексов, сервис синхронизации, REST API `/media-assets` и CLI-команда.

### Добавлено

**Клиент Яндекс Диска:**
- Клиент `backend/app/integrations/yandex_disk/client.py` — обращение к REST API Яндекс Диска через **httpx** и OAuth-токен (режим read-only: файлы целиком на этом этапе не скачиваются). `dataclass YandexDiskResource` (`name`, `path`, `type`, `mime_type`, `size`, `modified`, `public_url`; свойства `is_file`/`is_dir`).
- Методы: `list_resources` (`GET /resources`, парсит `_embedded.items`), `list_files_recursive` (рекурсивный обход, возвращает только файлы, `max_depth=3`), `get_download_url` (`GET /resources/download` → `href`).
- Обработка ошибок: `YandexDiskError`, `YandexDiskAuthError` (пустой токен, HTTP `401`/`403`), `YandexDiskNotFoundError` (HTTP `404`). В тестах сеть не задействуется — передаётся `transport=httpx.MockTransport`.

**Сопоставление папок проектов:**
- `backend/app/services/project_media_paths.py`: `get_project_disk_root(slug)` (`teeon` → `/SMM_BOT/01_TEEON`, `fabric-souvenirs` → `/SMM_BOT/02_Фабрика_сувениров`), `get_default_scan_folders(slug)` → 4 полных пути (`01_Входящие_на_разбор`, `02_Одобренные_фото`, `03_Видео`, `04_Внешние_картинки_из_интернета`). Неизвестный `slug` → `UnknownProjectError`.

**Доступ к данным:**
- Репозиторий `backend/app/repositories/media_asset_repository.py`: `get_media_asset_by_id`, `get_media_asset_by_path`, `list_media_assets(project_id, status, limit, offset)`, `create_media_asset`, `update_media_asset`, `upsert_media_asset_from_disk_resource` (бизнес-уникальность по `yandex_disk_path`; возвращает `(asset, action)`, где `action ∈ created/updated/unchanged` — повторная синхронизация не плодит дубликаты).
- Схемы `backend/app/schemas/media_asset.py`: `MediaAssetRead`, `MediaAssetCreate`, `MediaAssetUpdate` (все поля опциональны), `MediaAssetSyncResult` (`project_id`, `project_slug`, `scanned_folders`, `found_files`, `created`, `updated`, `skipped`, `errors`).

**Миграция БД (Alembic):**
- Миграция `0002_media_asset_disk_path_indexes`: уникальный индекс `ix_media_assets_yandex_disk_path`; индексы `ix_media_assets_status`, `ix_media_assets_source_type`. Совместима с PostgreSQL; миграция `0001` не изменялась; проверена offline-генерацией SQL (корректный PostgreSQL DDL).
- Модель `MediaAsset` уточнена: `yandex_disk_path` — `unique + index`; `source_type ∈ {internal, external_stock}`; `license_type ∈ {company_owned, external_needs_review, commercial_use_required}`; `status ∈ {new, approved, approved_video, needs_license_review, used}`.

**Сервис синхронизации:**
- `backend/app/services/yandex_disk_media_sync_service.py`: `YandexDiskMediaSyncService(client, tagging_service)` с методами `sync_project_media(db, project_id)` и `sync_project_media_by_slug(db, slug)`.
- Логика: найти проект (нет → `ProjectNotFoundError`); получить папки сканирования; для каждой папки `list_files_recursive` (папка не найдена → запись в `errors`, синхронизация продолжается; `YandexDiskAuthError` → прерывание всей синхронизации); классификация по пути (`04_Внешние_картинки_из_интернета` → `source_type=external_stock`/`license_type=external_needs_review`/`status=needs_license_review`; `02_Одобренные_фото` → `status=approved`; `03_Видео` → `status=approved_video`; иначе → `internal`/`company_owned`/`new`); теги через `MediaTaggingService.extract_tags_from_file_name`; `upsert` по `yandex_disk_path`. Возвращает `MediaAssetSyncResult`.

**REST API для MediaAsset:**
- Роутер `backend/app/api/media_assets.py` с префиксом `/media-assets`, подключён в `main.py`. Зависимости `get_yandex_disk_client` / `get_media_sync_service` в `backend/app/api/deps.py` (в тестах подменяются).
- Эндпоинты: `GET /media-assets?project_id=&status=&limit=&offset=` (список, работает без токена); `GET /media-assets/{id}` (`404`, если нет); `POST /media-assets/sync/project/{project_id}` и `POST /media-assets/sync/slug/{slug}` → `MediaAssetSyncResult` (`404` — нет проекта; `503` — нет/отклонён токен; `400` — неизвестная папка проекта). Реальные запросы к Диску — только при `sync`.

**CLI и Makefile:**
- Скрипт `backend/app/scripts/sync_media.py` и команда `make sync-media project_slug=teeon` (печатает отчёт `scanned_folders`/`found`/`created`/`updated`/`skipped`/`errors`; без токена — понятное сообщение). Не входит в `make check`.

**Конфигурация:**
- В `app/config.py` и `.env.example`: `YANDEX_DISK_TOKEN` (`str | None`, OAuth-токен), `YANDEX_DISK_BASE_URL=https://cloud-api.yandex.net/v1/disk`, `YANDEX_DISK_ROOT_PATH=/SMM_BOT`. Без токена обычный API работает; sync-эндпоинты возвращают `503`.

**Тесты:**
- Тесты используют **SQLite в памяти** и mock-транспорт — без сети, Docker и PostgreSQL. Покрыто: клиент Яндекс Диска (парсинг `_embedded.items`, рекурсивный обход только файлов, `404` → `NotFound`, пустой токен → `AuthError`, `download href`); `project_media_paths`; репозиторий `MediaAsset` (create/get_by_path/upsert без дублей/фильтры); сервис синхронизации (создание `MediaAsset`, идемпотентность, теги, `external` → `external_stock`/`needs_license_review`, отсутствующая папка → `errors` с продолжением, неизвестный проект → `ProjectNotFoundError`); API `/media-assets` (list/get/`404`/фильтр/sync с mock/`404`/без токена → `503`). Старые тесты Этапов 0 и 1 сохранены. Всего **60 тестов** — все проходят.

### Состояние

`make format` и `make check` (ruff + mypy на 51 файле + pytest, 60 тестов) проходят успешно. Live-смоук на временной БД: `GET /media-assets` работает без токена, `sync` без токена → `503`, отсутствующий проект → `404`. Docker и реальный Яндекс Диск в среде недоступны — не использовались.

Порядок ручной синхронизации (локально): `make db-up`; `make migrate`; `make seed-projects`; `make sync-media project_slug=teeon`. Для реальной синхронизации нужен `YANDEX_DISK_TOKEN` в `.env`.

### Следующий шаг

Переход к **Этапу 3** — развитие бизнес-логики поверх синхронизированных медиаресурсов.

---

## 2026-06-18 — Этап 1 — Проекты

Завершён **Этап 1** (поверх Этапа 0). Реализована полноценная работа с сущностью `Project`: миграции БД, репозиторий и REST API с CRUD (без удаления из БД), а также идемпотентный seed стартовых проектов.

### Добавлено

**Миграции БД (Alembic):**
- Подключён **Alembic**: файл `alembic.ini` в корне; пакет `backend/alembic/` (`env.py` берёт URL подключения из настроек приложения и `target_metadata` из `app.models`; `script.py.mako`; каталог `versions/`).
- Первая миграция `0001_initial` создаёт таблицы `projects`, `media_assets`, `topics`, `posts`. JSON-поля используют `JSONB` на PostgreSQL; внешние ключи: `ON DELETE CASCADE` для `project_id`, `ON DELETE SET NULL` для `topic_id` и `media_asset_id`; уникальный индекс `ix_projects_slug`; индексы по внешним ключам; `created_at`/`updated_at` с `server_default now()`.
- Миграция дополнительно проверена offline-генерацией SQL (корректный PostgreSQL DDL: `JSONB`, FK, индексы).

**Доступ к данным:**
- DB-зависимость `get_db()` в `backend/app/api/deps.py` — выдаёт SQLAlchemy `Session` и закрывает её после запроса (делегирует в `app/db/session.py`).
- Репозиторий `backend/app/repositories/project_repository.py`: `get_project_by_id`, `get_project_by_slug`, `list_projects(active_only=True)`, `create_project`, `update_project` (частичное обновление через `exclude_unset`), `deactivate_project` (ставит `is_active=False`, запись не удаляется). Дубликат `slug` приводит к исключению `SlugAlreadyExistsError`.
- Схемы `backend/app/schemas/project.py`: `ProjectCreate`, `ProjectUpdate` (все поля опциональны), `ProjectRead` (используется и для списка). Валидация `slug`: разрешены только латиница, цифры, `-` и `_`; значение нормализуется к нижнему регистру (`TEEON` → `teeon`); пробелы и кириллица отклоняются (`422`); минимальная длина 2 символа.

**REST API для Project:**
- Роутер `backend/app/api/projects.py` с префиксом `/projects`, подключён в `main.py`. Все ответы типизированы через `response_model` (`ProjectRead`). Удаления из БД на этом этапе нет.
- Эндпоинты: `GET /projects?active_only=true` (список); `POST /projects` (создать, `201`; дубль `slug` → `409`; невалидный `slug` → `422`); `GET /projects/slug/{slug}` (`404`, если нет); `GET /projects/{project_id}` (`404`); `PATCH /projects/{project_id}` (частичное обновление; `404`; занятый `slug` → `409`); `POST /projects/{project_id}/deactivate` (`404`).

**Seed:**
- Скрипт `backend/app/scripts/seed_projects.py` и команда `make seed-projects`. Идемпотентно (не плодит дубликаты, печатает отчёт «создано / уже существует»). Проекты: **TEEON** (`slug=teeon`, `website_url=https://teeon.ru`) и **«Фабрика сувениров»** (`slug=fabric-souvenirs`, `website_url=null`).

**Makefile:**
- Добавлены команды: `db-up` (`docker compose up -d db redis`), `db-down`, `migrate` (`alembic upgrade head`), `revision` (`make revision message="..."`), `seed-projects`.

**Тесты:**
- Тесты используют **SQLite в памяти**, поэтому `make check` не зависит от запущенного Docker/PostgreSQL. `conftest.py` создаёт схему и переопределяет зависимость `get_db`.
- Покрыто: `/health`; Project API (создание, дубль `slug` → `409`, get by id, get by slug, список, фильтр `active_only`, частичное обновление, обновление `slug` на занятый → `409`, деактивация, `404` для несуществующих, `422` для невалидного `slug`); валидация `slug`; репозиторий (`get_project_by_slug`, проверка, что `deactivate` не удаляет запись). Всего **34 теста** — все проходят. Старые тесты Этапа 0 (включая `MediaTaggingService`) сохранены.

### Состояние

`make format` и `make check` (ruff + mypy на 46 файлах + pytest, 34 теста) проходят успешно. Seed и API дополнительно проверены вживую.

Порядок локального запуска: 1) `make install`; 2) `cp .env.example .env`; 3) `make db-up`; 4) `make migrate`; 5) `make seed-projects`; 6) `make run`.

### Следующий шаг

Переход к **Этапу 2** — развитие бизнес-логики поверх сущности `Project`.

---

## 2026-06-18 — Этап 0 — Каркас проекта

Начат **Этап 0**. Заложен технический фундамент проекта, на который будут опираться все последующие этапы (от работы с проектами до автономного режима).

### Добавлено

**Каркас проекта:**
- Структура каталогов репозитория: документация в `Докс/`, бэкенд в `backend/` с пакетом `app` (`api`, `core`, `db`, `models`, `schemas`, `services`, `integrations`, `ai`, `scheduler`, `utils`) и тестами в `backend/tests`.
- FastAPI-приложение с эндпоинтом `GET /health`, возвращающим `{"status": "ok", "service": "ai-smm-bot"}`.
- Конфигурация на **Pydantic Settings** (значения берутся из окружения и `.env`; токены в коде не хранятся).
- SQLAlchemy-модели: `Project`, `MediaAsset`, `Topic`, `Post` (с ключевыми полями и связями FK между сущностями).
- Pydantic-схемы для перечисленных моделей (валидация и сериализация данных API).
- Сервисы-заглушки бизнес-логики: `TopicSelectionService`, `MediaTaggingService` (с рабочей логикой разбора имени файла), `PostGenerationService`.
- Клиенты-заглушки внешних интеграций в `app/integrations`: Яндекс Диск, Telegram, VK, Instagram, AI.
- Инфраструктура: `Dockerfile`, `docker-compose` (postgres + redis + backend), `Makefile` (цели `install` / `run` / `test` / `lint` / `format` / `typecheck` / `check`), `pyproject.toml` (настройки ruff / mypy / pytest), файл `.env.example`.

**Проверки качества:**
- Подключён **ruff** — линт и форматирование кода.
- Подключён **mypy** — статическая проверка типов.
- Подключён **pytest** — автотесты (health, конфигурация, текстовые утилиты, разбор имени медиафайла).
- Добавлена сводная команда `make check`, которая запускает `ruff check` + `mypy` + `pytest`. Перед переходом к следующему этапу `make check` должна проходить без ошибок.

**Документация:**
- В папке `Докс/` создана документация проекта, включая данный журнал изменений.

### Состояние

Каркас собирается и запускается, проверки `make check` проходят. Сервисы пока являются заглушками (кроме рабочего разбора имени файла в `MediaTaggingService`); реальная бизнес-логика будет наполняться на следующих этапах.

### Следующий шаг

Переход к **Этапу 1 — Проекты**: реализация CRUD для сущности `Project`, REST-эндпоинтов и сервисного слоя работы с проектами компании.

---

<!--
ШАБЛОН ДЛЯ БУДУЩИХ ЗАПИСЕЙ (копируйте блок ниже и заполняйте, размещая НАД предыдущими записями):

## ГГГГ-ММ-ДД — Этап N — Краткое название

Краткое описание сути этапа/изменения (1–2 предложения).

### Добавлено
- ...

### Изменено
- ...

### Исправлено
- ...

### Удалено
- ...

### Состояние
- Результат `make check`, что работает, что осталось заглушкой.

### Следующий шаг
- Переход к Этапу N+1 — ...

---
-->
