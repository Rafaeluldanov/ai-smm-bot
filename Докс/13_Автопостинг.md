# Автопостинг (Этап 7)

Документ описывает backend-логику **планирования и публикации** одобренных постов в Telegram и VK. Публикация идёт через изолированные клиенты, которые легко подменяются на фейковые в тестах. Реальные сетевые вызовы и Instagram на этом этапе **не** подключены; аналитика эффективности — Этап 8.

> **VK подключается через кнопку OAuth, а не через сервисный ключ (v0.2.5).** Для
> загрузки фото на стену нужен **пользовательский** VK-токен (`photos.getWallUploadServer`
> недоступен групповому токену — VK `error_code=27`). Токен выдаётся не вручную, а через
> кнопку «Подключить VK» в дашборде проекта: OAuth-поток
> `GET /integrations/vk/oauth/start` → авторизация VK (scope `wall,photos,groups,offline`)
> → `GET /integrations/vk/oauth/callback` обменивает `code` на user-token и кладёт его в
> **секрет ресурса** (наружу — только маска). Затем safe-check `users.get` /
> `groups.get filter=admin` / `photos.getWallUploadServer` (без публикаций). Настройки:
> `VK_APP_ID`, `VK_APP_SECRET`, `VK_OAUTH_REDIRECT_URI`. Подробнее — Докс/25. VK
> live-публикация по-прежнему выключена по умолчанию (`VK_LIVE_PUBLISHING_ENABLED`).
>
> **Готовый сценарий (v0.2.6, локальный HTTPS):** VK ID требует HTTPS redirect, поэтому
> для локального подключения поднимаем self-signed HTTPS на `https://localhost:8443`:
> `make local-https-cert` → `make vk-oauth-local-https` (мастер `.env`, секрет через
> `getpass`, live не включает) → `make run-https-local`. В VK ID приложении: базовый
> домен `localhost`, Redirect URL `https://localhost:8443/integrations/vk/oauth/callback`
> (запасной — `127.0.0.1` / `https://127.0.0.1:8443/...`). Затем в UI по
> `https://localhost:8443/ui/projects`: TEEON → VK → «Подключить VK» → «Проверить доступ».
> При «Загрузка фото ✔» — `make vk-photo-test-preview`/`-apply` готовит VK media-group
> пост с картинками (`needs_review`, `platform_target=vk`) без публикации. Затем вручную:
> `review_post approve` → `schedule_post --platform vk` → `publish_post --platform vk
> --dry-run`; **реальный live VK — только отдельной ручной командой после подтверждения**.
>
> **VK browser publisher fallback (v0.2.7, dev/local):** пока нет рабочего VK user-token
> (ключ сообщества не грузит фото — error 27), пост с картинками можно опубликовать
> через локальную автоматизацию браузера — `app.scripts.vk_browser_publish_post`,
> **без API-токенов VK**. Скрипт берёт `Post` из БД и его
> `generation_notes.media_files`, готовит картинки (Яндекс Диск → JPEG, HEIC-конвертация)
> в `tmp/vk_browser_uploads/`, открывает VK через Playwright (persistent profile), где
> **пользователь логинится вручную** (пароль не хранится), вставляет текст и прикрепляет
> файлы. По умолчанию **dry-run** (публикация не нажимается); live — только
> `make vk-browser-publish-live post_id=… ` (`--confirm-live true`). Установка dev-
> инструмента — `make vk-browser-install`. Это **не** SaaS production flow (там —
> официальный OAuth user-token); токены/секреты не печатаются, `VK_ACCESS_TOKEN` не нужен.

## VK user-token и публичный HTTPS callback (v0.2.9)

Для VK-фото через API нужен **пользовательский** VK-токен. Текущий community/group
token не подходит: `users.get → []`, `groups.get filter=admin → error 27`,
`photos.*` (wall/album) → `error 27`. Для продакшн-автоматики нужен **публичный HTTPS
OAuth callback** (не туннель, не браузер):

- `PUBLIC_APP_URL=https://app.teeon.ru` → `VK_OAUTH_REDIRECT_URI` выводится как
  `https://app.teeon.ru/integrations/vk/oauth/callback` (если не задан явно).
- В VK ID приложении: базовый домен `app.teeon.ru`, Redirect URL — этот адрес.
  Подсказку без секретов печатает `make vk-oauth-setup-info`.
- В UI: «Подключить VK» → авторизация → callback сохраняет user-token в секрет ресурса
  (наружу только маска) → «Проверить доступ» (`users.get`/`groups.get filter=admin`/
  `photos.getWallUploadServer`). При провале UI пишет: «Токен не пользовательский или
  аккаунт не имеет прав администратора/редактора группы.»
- После подключения user-token `make vk-api-photo-probe-upload` даёт
  `RECOMMENDED_STRATEGY=wall|album`, и календарь публикует VK с картинками через API.

Endpoints: `GET /integrations/vk/oauth/start`, `GET /integrations/vk/oauth/callback`,
`GET /integrations/vk/status`, `POST /integrations/vk/oauth/check`. Токены не логируются.

## VK API photo upload strategies (v0.2.8)

Полная автоматика (календарь) публикует VK-фото **через API**, две стратегии:

- **wall** — `photos.getWallUploadServer` / `photos.saveWallPhoto`. Community-token
  часто падает `error_code=27`.
- **album** — `photos.getUploadServer` / `photos.save` в альбом группы (альбом ищется
  по `VK_PHOTO_ALBUM_TITLE` или создаётся). Затем `wall.post` с `attachments=photo{owner}_{id}`,
  `owner_id=-group_id`, `from_group=1`.
- **auto** (по умолчанию `VK_PHOTO_UPLOAD_STRATEGY`) — wall → album при error 27.

Если album тоже даёт `27/15/5`: для обычного поста — безопасный text-only фолбэк с
`raw.upload_strategy` / `upload_error_code` / `media_upload_skipped=true`; для поста с
`generation_notes.media_policy=media_group` — **PublishError** (календарь не публикует
пост без обязательных картинок). Токен VK не логируется ни в raw, ни в исключениях.

Диагностика токена — `make vk-api-photo-probe` (read-only) / `make vk-api-photo-probe-upload`
(реальная загрузка тестового фото, но **без** `wall.post`); печатает
`RECOMMENDED_STRATEGY=wall|album|none`. Браузерный publisher — **не** основной способ
календарной публикации.

## Жизненный цикл: approved → scheduled → published

```
approved ──schedule──► scheduled ──publish (все платформы ok)──► published
                          │                                         ▲
                          │ publish (часть ошибок)                  │
                          └──────────── остаётся scheduled ─────────┘
```

1. **schedule** (`PostPublicationService.schedule_post`): по `approved`/`scheduled` посту создаёт/обновляет `PostPublication` для каждой платформы; `Post.status → scheduled` (и `Post.scheduled_at`, если задан). Публикации получают статус `scheduled` (с датой) или `pending` (без даты — публиковать сразу).
2. **publish** (`publish_post`): публикует пост по платформам. При успехе **всех** платформ без ошибок `Post.status → published` (через `scheduled`), `Post.published_at` проставляется. Если есть ошибки — пост остаётся `scheduled`.
3. **publish-due** (`publish_due_publications`): находит «созревшие» публикации и публикует их (используется планировщиком).

Из каких статусов можно: планировать — `approved`/`scheduled`; публиковать — `approved`/`scheduled`/`published` (последнее — чтобы корректно отдавать `skipped`/`force`). Иначе — `PostNotPublishableError` (API → 409).

## Telegram и VK

Публикация абстрагирована от платформы:

- `PublishRequest` (платформа, `target_id`, текст, медиа, хэштеги, payload) и `PublishResponse` (`external_post_id`, `external_url`, `raw`) — `backend/app/integrations/publishing.py`.
- Клиенты реализуют протокол `PublishingClient.publish_post(request) → PublishResponse`.
- `TelegramPublishingClient` и `VKPublishingClient` — безопасные: реальная отправка включается **только** флагом `live_enabled` (см. ниже), без токена/таргета — `PublishError`. В тестах используется `FakePublishingClient` либо реальный клиент с подменённым HTTP-транспортом (`httpx.MockTransport`).
- `PublicationPlatformRegistry` сопоставляет платформу с клиентом (`get_client`, `get_available_platforms`); неизвестная платформа → `UnknownPlatformError`.

Для Telegram берётся `post.telegram_text`, для VK — `post.vk_text`; хэштеги кладутся в `payload`; `target_id` — из публикации (заданный при планировании) или из настроек по умолчанию (`TELEGRAM_DEFAULT_CHANNEL_ID` / `VK_DEFAULT_GROUP_ID`).

**Медиа.** Если у `MediaAsset` поста есть **одобренный** (`approved`) улучшенный вариант (`MediaAssetVariant`), в запрос кладётся путь к улучшенной копии как `preferred_media_path` (`media_path`), а `payload.media_source = "enhanced_variant"`. Иначе — метаданные оригинала (`payload.media_source = "original"`, `attachment` с `file_name`/`yandex_disk_path`). В `payload` также кладётся `media_kind` (`image` | `video` | `none`).

**Загрузка фото во VK (v0.1.12).** При живой публикации `VKPublishingClient` прикрепляет изображение: читает байты из локального файла улучшенной копии (`media_path`) либо скачивает оригинал из публичной папки Яндекс Диска (`public://yandex/...`), затем `photos.getWallUploadServer` → multipart-upload → `photos.saveWallPhoto` → `attachments=photo{owner_id}_{id}` в `wall.post`.

- **Видео** (`.mov`/`.mp4`/…) пока не загружается: пост уходит текстом, предупреждение — в `PublishResponse.raw.media_warnings`.
- **Групповой токен и фото (VK error 27).** VK-метод `photos.getWallUploadServer`/`photos.saveWallPhoto` **недоступен с групповым токеном** (`Group authorization failed`, `error_code=27`). Текстовая публикация (`wall.post`) с групповым токеном работает. Поэтому при `error_code=27` бот **не роняет публикацию**, а делает **text-only fallback**: постит текст без вложения и добавляет в `raw`:
  ```json
  {
    "media_upload_skipped": true,
    "media_upload_error_code": 27,
    "media_warnings": ["VK photo upload skipped: group token cannot call photos.getWallUploadServer/photos.saveWallPhoto"]
  }
  ```
  Любая **другая** ошибка upload flow остаётся `PublishError` (публикация помечается `failed`).
- **Полноценная загрузка фото через user-token** (или через отдельный сервис аплоада) — задача будущего этапа. Токен нигде не логируется и не попадает в `raw`/тексты ошибок.

## Безопасная live-публикация (флаги)

Реальная отправка в соцсети **по умолчанию выключена** — без явных флагов опубликовать ничего нельзя (защита от случайной публикации):

| Флаг (`.env`) | По умолчанию | Что включает |
|---------------|--------------|--------------|
| `TELEGRAM_LIVE_PUBLISHING_ENABLED` | `false` | Реальный вызов Telegram Bot API `sendMessage` |
| `VK_LIVE_PUBLISHING_ENABLED` | `false` | Реальный вызов VK `wall.post` |

- **Флаг `false`:** `publish` возвращает `PublishError` «Live publishing disabled by config», **сеть не вызывается**, публикация помечается `failed` с этим сообщением.
- **Флаг `true`:** Telegram использует `TELEGRAM_BOT_TOKEN` + `TELEGRAM_DEFAULT_CHANNEL_ID`, VK — `VK_ACCESS_TOKEN` + `VK_DEFAULT_GROUP_ID`. `GET /health/readiness` предупредит, если флаг включён, но токен/таргет не заданы.
- В тестах реальные HTTP-вызовы Telegram/VK всегда мокаются (`httpx.MockTransport`) — настоящая сеть не дёргается.

**Dry-run preview.** Можно посмотреть, что и куда ушло бы, **без отправки**:

- API: `POST /post-publications/preview/{post_id}` → платформы, `target_id`, текст, `preferred_media_path`, `media_source`, `media_kind`, `would_attach_media`, `live_enabled` (ничего не публикует, сеть не вызывается);
- CLI: `python -m app.scripts.publish_post --post-id 1 --dry-run`.

## PostPublication

Каждая публикация — строка `post_publications` (модель `PostPublication`): платформа, цель (`target_id`), статус, внешний id/url, время плана/публикации, ошибка, число попыток (`attempts`) и `payload` (текст, `media_asset_id`, attachment, raw-ответ). Подробности модели — в [`Докс/06_Модель_данных.md`](./06_Модель_данных.md) (раздел «PostPublication»).

## Идемпотентность

- Уникальный индекс `ix_post_publications_post_id_platform` (`post_id` + `platform`) — один пост не публикуется в одну платформу дважды.
- `upsert_publication_schedule` обновляет существующую запись, не плодя дубли; уже опубликованную (`published`) не перезаписывает.
- При публикации: если публикация уже `published` и `force=false` — она помечается `skipped` (не публикуется повторно). С `force=true` — публикуется заново (растёт `attempts`).

## Ошибки публикации

Ошибка одной платформы **не** роняет весь процесс: соответствующая публикация получает `status=failed` и `error_message`, остальные платформы обрабатываются как обычно. `Post.status` переходит в `published` только если успешны все платформы и нет ошибок. На уровне API `publish` возвращает 200 со счётчиками `published_count`/`failed_count`/`skipped_count` — частичные сбои видны в ответе, а не как 5xx.

## Планировщик

`publish_due_publications_job(db, service, now=None)` (`app/scheduler/jobs.py`) делает один проход: берёт созревшие публикации (`scheduled`/`pending` с `scheduled_at <= now` или без даты), группирует по посту и публикует. Без бесконечного цикла и без сети (клиенты подменяемы) — задача легко тестируется и переиспользуется из CLI.

## API и команды

REST API `/post-publications` (`backend/app/api/post_publications.py`):

- `GET /post-publications?post_id=&project_id=&platform=&status=&limit=&offset=` — список;
- `GET /post-publications/{publication_id}` — публикация по id (404);
- `POST /post-publications/schedule/{post_id}` — запланировать (404/409);
- `POST /post-publications/publish/{post_id}` — опубликовать (404/409; частичные сбои — в теле);
- `POST /post-publications/publish-due` — опубликовать созревшие (тело `{ "now": "..." }` опционально);
- `POST /post-publications/preview/{post_id}` — **dry-run preview** payload без отправки (404 — нет поста);
- `PATCH /post-publications/{publication_id}` — ручная правка (`target_id`/`status`/`error_message` и пр.).

CLI:

```bash
make schedule-post post_id=1
make publish-post post_id=1
make publish-due
```

## SEO-ссылка на сайт в тексте поста

`PostGenerationService` добавляет в конец VK/Telegram-текста **одну** релевантную
ссылку на страницу [teeon.ru](https://teeon.ru) (`Подробнее и расчёт тиража: …`).
Ссылку выбирает `site_link_selection_service` по теме/тегам/технологии/продукту.
Подробнее — [20_SEO_VK_группа_TEEON.md](20_SEO_VK_группа_TEEON.md).

## SEO-заполнение VK-группы (preview) отдельно от публикации

Оформление группы (название/описание/статус/закреп/меню) готовит
`vk_group_seo_setup_service` — **только preview/dry-run**, без вызовов VK API.
Реальные изменения оформления выключены флагом `VK_GROUP_SETUP_LIVE_ENABLED=false`
(независимо от `VK_LIVE_PUBLISHING_ENABLED`). См.
[20_SEO_VK_группа_TEEON.md](20_SEO_VK_группа_TEEON.md).

## Медиа-вложения и фотоальбомы

Пост с группой медиа (несколько фото по общему тегу) публикуется одним постом с
несколькими фото — и в VK, и в Telegram:

- **VK** — фото загружаются через `photos.getWallUploadServer` → upload →
  `photos.saveWallPhoto`, вложения `photo{owner}_{id}` через запятую в `wall.post`
  (лимит `VK_MEDIA_GROUP_MAX_PHOTOS`, по умолчанию 5). Групповой токен без прав
  `photos.*` (error_code=27) → безопасный text-only фолбэк.
- **Telegram** — фотоальбом через `sendMediaGroup` (2–10 фото, caption только в
  первом элементе) или `sendPhoto` для одного фото (лимит
  `TELEGRAM_MEDIA_GROUP_MAX_PHOTOS`, по умолчанию 10).
- Общее: HEIC/HEIF конвертируется в JPEG **в памяти** (оригинал не
  перезаписывается); видео пока пропускается с предупреждением; байты берутся из
  локальной enhanced-копии или публичной папки Яндекс Диска. Загрузка медиа и
  конвертация — в общем модуле `app/integrations/media_attachments.py`.

Подробно: VK — [22_Группировка_медиа_и_VK_посты.md](22_Группировка_медиа_и_VK_посты.md),
Telegram — [23_Telegram_media_group.md](23_Telegram_media_group.md).

## Мультиплатформенность (capability-слой)

Возможности платформ (текст/фото/альбом/видео, лимиты, live-флаг) вынесены в
`app/integrations/platform_capabilities.py`. `route_media` решает, что уйдёт на
каждую платформу; dry-run/preview показывает `would_attach_media`,
`media_warnings`, `unsupported_media_reason`, `platform_capabilities`.

- **Live-ready:** VK, Telegram (текст + фото-группа).
- **Skeleton / dry-run:** Instagram (фото/carousel/reels), YouTube и RuTube (видео).
  Live возвращает `Live publishing for <platform> is not implemented yet`.

Подробно — [24_Мультиплатформенная_публикация_медиа.md](24_Мультиплатформенная_публикация_медиа.md).

## Подключение платформ в Botfleet

Раздел «Гайд» (`/ui/guide`) и карточки дашборда подробно объясняют подключение каждой
площадки; всё безопасно (preview/dry-run, без реальных вызовов).

- **Telegram** — бот через **BotFather**, добавить администратором канала, указать
  `@username`/числовой id. Частые проблемы: `chat not found` (бот не в канале / неверный
  chat_id), неверный токен, невключённый live-флаг.
- **VK** — `group_id`, для фото через API нужен личный **user-token** (не ключ
  сообщества). Частые проблемы: `error 27` (ключ сообщества не грузит фото → user-token
  или стратегия album), OAuth требует **публичный HTTPS-домен** в Redirect URL.
- **Instagram** — Professional-аккаунт, **Facebook Page** → **Meta Developer** (Meta App)
  → **Graph API**; запасной путь «**Instagram API with Instagram Login**». Нужны App ID,
  App Secret (секрет), Redirect URI, Access Token (секрет), Instagram User ID. Публикация:
  `POST /{ig-user-id}/media` (контейнер с **публичным image_url**) → `media_publish`.
  На дашборде — справочная карточка Instagram (секреты только маской), live выключен.
  Instagram dry-run preview проставляет `needs_public_image_url`/`would_prepare_media`.

## Юнит-экономика и стоимость публикации (v0.2.12)

Стоимость действий в units считает `unit_economics_service.py` из токенов провайдера
(`AI_INPUT_USD_PER_1M`/`AI_OUTPUT_USD_PER_1M`) × наценка (`BILLING_MARKUP_MULTIPLIER`),
с минимальным порогом. Публикация: text-only 2, с медиа 3, Instagram 4 units;
генерация короткого поста — от 5. Списание — через `BillingService.reserve_or_debit`
(идемпотентно, не в минус): публикация списывает только после успеха, dry-run — 0 units,
неуспех не списывает, повтор не списывает дважды. Витрина цен — `/ui/tariffs`, подробно —
[26_Botfleet_SaaS_UX_платформы_расписания_аналитика.md](26_Botfleet_SaaS_UX_платформы_расписания_аналитика.md).
Расписания показываются как отдельные задачи внутри платформы (дашборд отдаёт их в
`extra.schedule_tasks`, без новой миграции).

## Ограничения

- **Live-ready — только Telegram и VK**; Instagram/YouTube/RuTube — adapter-скелеты
  (preview работает, live пока не реализован);
- **в тестах нет реальных API и сети** — `FakePublishingClient` или `httpx.MockTransport`;
- live-публикация выключена по умолчанию: без `*_LIVE_PUBLISHING_ENABLED` отправка невозможна;
- SEO-заполнение группы выключено по умолчанию: без `VK_GROUP_SETUP_LIVE_ENABLED` — только preview;
- **видео** пока не загружается ни в VK, ни в Telegram — пропускается с предупреждением;
- **аналитика эффективности постов** — Этап 8.

## Аналитика постов и платежи (v0.2.13)

Глубокая аналитика постов (light/standard/deep = 10/20/40 units за пост): анализ контента,
estimated-метрики, рекомендации, календарь, ручной ввод метрик (0 units). Платёжный слой
для России (карта/СБП/QR/счёт для ИП-ООО) — mock/sandbox, реальные платежи выключены
(`PAYMENTS_LIVE_ENABLED=false`). Подробно —
[27_Botfleet_аналитика_и_платежи.md](27_Botfleet_аналитика_и_платежи.md).

## Обновление v0.3.8: движок автоматизации расписаний

Расписания SaaS стали реальными задачами: Botfleet находит due-слоты и создаёт
**draft/needs_review** посты + публикации (pending/scheduled), списывает units и пишет логи
(`ScheduleRun`, миграция 0020). **Живой публикации нет** (это НЕ старый `publish-due`):
всё уходит на ревью, внешние API не вызываются, повтор идемпотентен. Подробно —
[35_Botfleet_Schedule_Automation_Engine.md](35_Botfleet_Schedule_Automation_Engine.md).

## Полу-/полностью автоматический режим (v0.4.0)

Клиент выбирает режим плана: `semi_auto` (создаётся `draft/needs_review`, публикует клиент
вручную кнопкой «Опубликовать») или `full_auto` (бот сам публикует, **только** при всех safety
gates — качество, баланс, подключение, live-флаг, первое одобрение; иначе `needs_review` с
причиной). `full_auto` требует подтверждения `ENABLE_FULL_AUTO` и **не** включает live-флаги.
Каждое решение клиента становится сигналом обучения. Подробно —
[37_Botfleet_Review_Learning_Automation.md](37_Botfleet_Review_Learning_Automation.md).

## Импорт метрик и обучение по результатам (v0.4.1)

После публикации бот собирает метрики постов (demo/manual/estimated/api), нормализует их к
единому формату и пересчитывает профиль обучения: какие темы/теги/CTA/медиа/время работают
лучше. Реальные API-метрики — за feature flag (выключены по умолчанию). Подробно —
[38_Botfleet_Metrics_Import_Learning_Feedback.md](38_Botfleet_Metrics_Import_Learning_Feedback.md).


## A/B-тесты и оптимизация тем (v0.4.2)

Перед ревью Botfleet может создать несколько вариантов поста (A/B/C) и после feedback+метрик
выбрать winner, который усиливает профиль обучения. Плюс рекомендации следующих тем.
Live-публикаций нет. Подробно —
[39_Botfleet_AB_Testing_Topic_Optimization.md](39_Botfleet_AB_Testing_Topic_Optimization.md).


> **Autopilot-first (v0.5.6):** этот слой скрыт за клиентским автопилотом — клиент подключает Яндекс Диск и календарь, а Botfleet сам всё делает. См. [53_Botfleet_Autopilot_First_Workspace.md](53_Botfleet_Autopilot_First_Workspace.md).

> **Yandex Disk auto-sync (v0.5.7):** клиент загружает картинки в Яндекс Диск — Botfleet сам синхронизирует медиатеку (dry-run/без сети по умолчанию, файлы не удаляются). См. [54_Botfleet_Yandex_Disk_Auto_Sync.md](54_Botfleet_Yandex_Disk_Auto_Sync.md).

> **Calendar assistant (v0.5.8):** клиент выбирает цель и частоту — Botfleet сам строит календарь автопостинга (`AutopilotCalendarPlan`) и по нажатию применяет его к автопилоту, создавая `CrmPublishingPlan`. Построение/применение не публикуют и не включают live-флаги. См. [55_Botfleet_Autopilot_Calendar_Assistant.md](55_Botfleet_Autopilot_Calendar_Assistant.md).